# Latency model fitting outputs

This folder can store fitted GPU profile parameters generated by:

```bash
python -m kvsched.cli microbench --out results/microbench.csv
# fill latency_ms measurements
python -m kvsched.cli fit-latency --in results/microbench.csv --out configs/latency/fitted_gpu_profiles.yaml
```
